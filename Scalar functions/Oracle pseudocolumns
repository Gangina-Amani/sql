CURRVAL and NEXTVAL
LEVEL
ROWID
ROWNUM

SELECT STUDENTSEQ.currval FROM DUAL;
INSERT INTO STUDENT VALUES (STUDENTSEQ.nextval, ‘AMANI’, ‘ORACLE’, 1947);

SELECT ROWID, ename FROM emp WHERE deptno = 30;

SELECT * FROM EMP WHERE ROWNUM <= 14; 
