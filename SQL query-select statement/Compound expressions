GROUP BY:
create view calculations as select deptno, min(sal) as "MINIMUM SAL", max(sal) as "MAXIMUM SAL", avg(sal) as "AVG SAL" from emp group by deptno;

ORDER BY:
SELECT rollno,sname, CONCAT('19K61A0',rollno) regid FROM student order by rollno;
